using System.Collections;
using System.Collections.Generic;
using UnityEngine;
/*
public class PowerUps : MonoBehaviour
{
    Vector3 originalPosition;
    private MoleBehaviour[] powerUpArray;
    float timeToAppear = 1.0f;
    // Start is called before the first frame update
    void Start()
    {
        originalPosition = transform.position;
        timeToAppear = Random.Range(2f, 50f);
        var chance = powerUpArray[Random.Range(0, powerUpArray.Length)];
    }

    // Update is called once per frame
    void Update()
    {
     Vector3 newPos = originalPosition;
     newPos.y -= 1.0f;
     LeanTween.move(gameObject, newPos, 0.24f);
     timeToAppear = Random.Range(minTime, maxTime);
    }
}
*/